#邀请后测试
# 以薄荷绿为例，你可以将 RGB 值替换为上述提到的其他颜色
r, g, b = 152, 251, 152
# 打印 16x16 的颜色方形
for _ in range(16):
    line = ""
    for _ in range(16):
        # 这里简单用一个字符表示一个像素，你可以根据喜好调整
        line += "\033[38;2;{};{};{}m*\033[0m".format(r, g, b)
    print(line)
